<div class="card m-3 mt-2 shadow p-2 mb-5 bg-body rounded" style="width: 18rem;">
	<a href="{{ path('product', {slug : product.slug}) }}"><img src="/uploads/{{product.illustration}}" class="card-img-top" style="height: 180px !important;" alt="{{ product.name }}"></a>
	<div class="card-body">
		<h5 class="card-title">{{ product.name|upper }}</h5>
		<span>{{ product.subtitle }}</span><br>
		<span>Prix:
			<strong>{{(product.price / 100) |number_format('2')}}</strong>
			€</span>
	</div>
</div>
